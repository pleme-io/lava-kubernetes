# lava-kubernetes

Autogenerated typed `(deflava-resource …)` shapes for every kubernetes
terraform-provider resource. Mirrors pangea-kubernetes.

## Regenerate

```bash
tofu providers schema -json > schema.json
lava-forge generate --schema schema.json --out resources
```
